# Workday Account (template)

Copy to `.cursor/skills/application-filler/assets/workday-account.md`. The live file is gitignored.

Every `myworkdayjobs.com` tenant (each employer's Workday site) requires its own account —
there is no single shared login like the aggregators in `.auth/`. Reuse the **same**
email/password on every tenant's Create Account form instead of generating one per employer.

| Field | Value |
|---|---|
| Email | |
| Password | |
